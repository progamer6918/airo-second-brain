# Chat Decisions — 2026-05-06

## Decision 1

GitHub handover ISSUE 001 through ISSUE 017 is complete and should be treated as historical workflow, not the active roadmap.

## Decision 2

The main repo focus is `~/earnsai-pulse-trading`, not `~/earnsai-telegram-gateway`.

## Decision 3

The valid current phase is Phase 9E Operator Readiness / Validating.

## Decision 4

The system is not yet claimed as 100% smooth dry-run. It is in validation before that point.

## Decision 5

Future new chats should prioritize `PROJECT_CARRY_OVER.md` and terminal snapshot output as the source of truth.

## Decision 6

Status konteks chat/shareability should start low in a new chat, around 5-10/100, then increase only as evidence accumulates.

## Decision 7

All project work remains paper-only, dry-run only, research-only, with no live trading and no private exchange API.

## Decision 8

The following operator commands now exist:

- `make paper-status`
- `make paper-report`
- `make paper-readiness`

## Decision 9

`make paper-status` and `make paper-report` have been validated. `make paper-report` did not dirty git status.

## Decision 10

Next work should improve controlled readiness and generated report cleanup before claiming smooth dry-run complete.
